name: Deploy Discord Bot to OCI (Prod)

on:
  push:
    branches:
      - prod

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: "chemuchi/maplebot-discordgo"

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Short SHA
        run: echo "SHORT_SHA=$(echo ${{ github.sha }} | cut -c1-7)" >> $GITHUB_ENV

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.SHORT_SHA }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Short SHA
        run: echo "SHORT_SHA=$(echo ${{ github.sha }} | cut -c1-7)" >> $GITHUB_ENV

      - name: Deploy to OCI VM via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          envs: SHORT_SHA
          script: |
            docker stop my-discord-bot || true
            docker rm my-discord-bot || true
            
            docker image prune -a -f
            
            docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${SHORT_SHA}
            docker run -d \
              --name my-discord-bot \
              --restart always \
              --memory="512m" \
              -e DISCORD_BOT_TOKEN="${{ secrets.DISCORD_BOT_TOKEN }}" \
              -e NEXON_API_KEY="${{ secrets.NEXON_API_KEY }}" \
              -e API_KEY_ENCRYPTION_KEY="${{ secrets.API_KEY_ENCRYPTION_KEY }}" \
              -e API_KEY_ENCRYPTION_PREVIOUS_KEYS="${{ secrets.API_KEY_ENCRYPTION_PREVIOUS_KEYS }}" \
              -e SUNDAY_CHANNEL_ID="${{ secrets.SUNDAY_CHANNEL_ID }}" \
              -e MYSQL_HOST="${{ secrets.MYSQL_HOST }}" \
              -e MYSQL_PORT="${{ secrets.MYSQL_PORT }}" \
              -e MYSQL_DATABASE="${{ secrets.MYSQL_DATABASE }}" \
              -e MYSQL_USER="${{ secrets.MYSQL_USER }}" \
              -e MYSQL_PASSWORD="${{ secrets.MYSQL_PASSWORD }}" \
              -e MYSQL_TLS_MODE="${{ secrets.MYSQL_TLS_MODE }}" \
              -e DATABASE_URL="${{ secrets.DATABASE_URL }}" \
              -e COMMIT_SHA="${SHORT_SHA}" \
              ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${SHORT_SHA}
